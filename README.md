# Quadratic equation unit tests

The solution contains the quadratic-equation program in `QuadraticEquation` and a separate xUnit project in `QuadraticEquation.Tests`.

```text
dotnet test QuadraticEquation.Tests/QuadraticEquation.Tests.csproj
```

The tests cover equations with no real roots, one real root, and two real roots.
